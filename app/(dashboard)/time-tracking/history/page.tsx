import { TimeHistory } from '@/features/time-tracking/components/time-history';

export const metadata = {
  title: 'Time History | TimeForge',
  description: 'View time history entries.',
};

export default function TimeHistoryPage() {
  return <TimeHistory />;
}
